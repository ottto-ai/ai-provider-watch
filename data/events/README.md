# Reviewed Events

Canonical event JSON files are stored here after maintainer review.

This initial foundation PR intentionally does not publish historical events until
the source registry and schema review are complete. Use `tests/fixtures/events/`
for validation examples.

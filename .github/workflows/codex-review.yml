name: Codex Review Prompt

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  prompt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: sed -n '1,220p' .github/codex/prompts/review-data-refresh.md

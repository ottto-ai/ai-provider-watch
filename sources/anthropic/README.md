# Anthropic Sources

Anthropic pricing and status sources. Pricing currently resolves under
`platform.claude.com`; status currently resolves under `status.claude.com`.
Legacy Anthropic hosts remain in `allowed_domains` only for official redirects.
Synthetic parser fixtures cover bounded Claude pricing/model signals and
Statuspage-style incident link hashing without copying incident titles.

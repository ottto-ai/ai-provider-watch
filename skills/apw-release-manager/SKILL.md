---
name: apw-release-manager
description: Prepare APW code or data releases with manifests, checksums, release notes, and validation evidence.
---

# APW Release Manager

Run `uv run pytest`, `uv run apw validate`, and `uv run apw index --check`. Confirm manifests, checksums, release notes, and token isolation before release.
